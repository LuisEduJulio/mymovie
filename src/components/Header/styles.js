import { StyleSheet } from 'react-native';

export const Styles = StyleSheet.create({
    container: {
        height: 90,
        backgroundColor: '#970E3E',
        display: 'flex',
        alignItems: 'flex-end'
    },
    Logo: {
        color: '#FFF'
    }
});