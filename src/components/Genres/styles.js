import { StyleSheet } from 'react-native';

export const Styles = StyleSheet.create({
    container: {
        margin: 10,
        borderRadius: 5,
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
    },
    Title: {
        color: '#000',
        fontSize: 20,
        fontWeight: 'bold',
        textAlign: 'center',
        marginTop: 10
    },
    Imagem: {
        height: 100,
        width: 100,
        marginTop: 10,
        borderRadius: 5,
        borderColor: '#FFF',
        borderWidth: 1,
        borderRadius: 100
    }
});